merge_method: task_arithmetic
base_model: base

models:
  - model: toolrl
    parameters:
      weight: 1.0
  - model: research
    parameters:
      weight: 1.0
  - model: gigpo-webshop
    parameters:
      weight: 1.0
  - model: gigpo-alfworld
    parameters:
      weight: 1.0

parameters:
  normalize: false
  lambda: 1.0

dtype: bfloat16
tokenizer:
  source: base
